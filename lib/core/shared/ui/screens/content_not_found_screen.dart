import 'package:flutter/material.dart';

class ContentNotFoundScreen extends StatelessWidget {
  const ContentNotFoundScreen({super.key});

  @override
  Widget build(BuildContext context) {
    return Scaffold();
  }
}
